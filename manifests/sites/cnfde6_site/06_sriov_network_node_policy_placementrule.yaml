apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: placement-cnfde6-site-sriov-nnode-policy
  namespace: cnfde6-site-subscription
spec:
  clusterSelector:
    matchExpressions:
      - {key: name, operator: In, values: ["cnfde6-cluster"]}
